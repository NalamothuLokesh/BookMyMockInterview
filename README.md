# Mock Interview App

Full-stack application for booking and conducting mock interviews.

## Structure

- `client/` - Vite React frontend
- `server/` - Node.js Express backend

## Setup

```bash
# Install all dependencies
npm run install-all

# Run both client and server
npm run dev
```

## Environment Variables

Create `.env` files in root and `client/` directories based on the provided templates.
